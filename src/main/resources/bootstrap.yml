spring:
  application:
    name: students-service
  profiles:
    active: dev
  config:
    uri: http://localhost:8888

server:
  port: 8000

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka